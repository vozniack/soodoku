spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa

  jpa:
    hibernate:
      ddl-auto: create

  flyway:
    enabled: false

logging:
  level:
    dev.vozniack.soodoku.core: DEBUG
